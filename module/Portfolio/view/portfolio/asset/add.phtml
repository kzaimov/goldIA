<?php
/** @var Laminas\View\Renderer\PhpRenderer $this */
$error = $this->error ?? null;
?>
<div class="ui container">
    <h2 class="ui header">Add Asset</h2>
    <form class="ui form" method="post" action="<?= $this->url('portfolio-assets/add') ?>">
        <div class="field">
            <label>Asset Name</label>
            <input type="text" name="name" required>
        </div>
        <div class="field">
            <label>Symbol</label>
            <input type="text" name="symbol" required>
        </div>
        <div class="field">
            <label>Initial Cost</label>
            <input type="number" name="initial_cost" step="0.01" min="0.01" required>
        </div>
        <div class="field">
            <label>Purchase Date</label>
            <input type="date" name="purchase_date" required>
        </div>
        <button class="ui primary button" type="submit">Save</button>
        <a class="ui button" href="<?= $this->url('portfolio-assets') ?>">Cancel</a>
    </form>
    <?php if ($error): ?>
        <div class="ui negative message" style="margin-top:1rem;"><?= $this->escapeHtml($error) ?></div>
    <?php endif; ?>
</div>
